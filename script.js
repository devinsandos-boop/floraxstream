document.querySelectorAll('.add').forEach(button=>button.addEventListener('click',()=>{button.textContent='✓';button.setAttribute('aria-label','Added to bag');setTimeout(()=>button.textContent='+',1600)}));
const form=document.querySelector('#contactForm');if(form)form.addEventListener('submit',e=>{e.preventDefault();document.querySelector('.success').style.display='block';form.reset()});
const newsletter=document.querySelector('#newsletterForm');if(newsletter)newsletter.addEventListener('submit',e=>{e.preventDefault();const b=newsletter.querySelector('button');b.textContent='Welcome!';newsletter.querySelector('input').value='';setTimeout(()=>b.textContent='Join us',2200)});
document.querySelectorAll('.filter').forEach(f=>f.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));f.classList.add('active');const q=f.dataset.filter;document.querySelectorAll('.shop-grid .product').forEach(p=>p.classList.toggle('hidden',q!=='all'&&!p.dataset.type.includes(q)))}));
